# Menu selector behavior

Ship and mode selectors cycle only through unlocked options.

When only one option is unlocked, the menu now shows progression status and tapping the selector explains the next unlock requirement instead of appearing inert.
