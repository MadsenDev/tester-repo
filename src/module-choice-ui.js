const LABELS={damage:"DAMAGE",cadence:"FIRE RATE",velocity:"SHOT SPEED",payload:"PAYLOAD",hull:"HULL",armor:"ARMOR",mobility:"MOBILITY",economy:"SALVAGE",crit:"CRIT",fork:"FORK",pierce:"PIERCE",seek:"SEEK",arc:"ARC",nova:"NOVA",anchor:"ANCHOR",prism:"PRISM",orbital:"ORBITAL",familiar:"FAMILIAR",synergy:"SYNERGY"};
const ICONS={damage:"✦",cadence:"»",velocity:"➤",payload:"◆",hull:"♥",armor:"⬡",mobility:"⌁",economy:"◇",crit:"✹",fork:"⑂",pierce:"◉",seek:"⌖",arc:"ϟ",nova:"✺",anchor:"◎",prism:"△",orbital:"◌",familiar:"✧",synergy:"∞"};
const fmt=(n,suffix="%")=>`${n>0?"+":""}${Math.round(n)}${suffix}`;
function effects(m){const e=m.effect||{},out=[];if(e.damage!=null)out.push(["damage","Damage",fmt((e.damage-1)*100)]);if(e.fireRate!=null)out.push(["cadence","Fire rate",fmt((1/e.fireRate-1)*100)]);if(e.bulletSpeed!=null)out.push(["velocity","Projectile speed",fmt((e.bulletSpeed-1)*100)]);if(e.bulletSize!=null)out.push(["payload","Projectile size",fmt((e.bulletSize-1)*100)]);if(e.speed!=null)out.push(["mobility","Movement speed",fmt((e.speed-1)*100)]);if(e.maxHp)out.push(["hull","Max hull",fmt(e.maxHp,"")]);if(e.heal)out.push(["hull","Repair hull",fmt(e.heal,"")]);if(e.regen)out.push(["hull","Hull regen",fmt(e.regen,"/s")]);if(e.armor)out.push(["armor","Damage resistance",fmt(e.armor*100)]);if(e.crit)out.push(["crit","Critical chance",fmt(e.crit*100)]);if(e.shots)out.push(["fork","Volley rounds",fmt(e.shots,"")]);if(e.pierce)out.push(["pierce","Pierce",fmt(e.pierce,"")]);if(e.magnet!=null)out.push(["economy","Pickup radius",fmt((e.magnet-1)*100)]);if(e.xpGain!=null)out.push(["economy","XP gain",fmt((e.xpGain-1)*100)]);if(e.companion)out.push([m.tags?.includes("orbital")?"orbital":"familiar",e.companion==="blade"?"Razor orbital":e.companion==="shield"?"Aegis orbital":e.companion==="ember"?"Ember familiar":e.companion==="wisp"?"Void familiar":"Gundrone","INSTALL"]);if(e.flags?.length)out.push([m.tags?.[0]||"synergy",`${LABELS[m.tags?.[0]]||"Combat"} behavior`,`ONLINE`]);if(e.special)out.push(["synergy","Rule-changing effect","ACTIVE"]);return out.slice(0,3)}
function tagScore(player,tag){const p=player?.passives||{};const alias={fork:"multishot",payload:"size",velocity:"bullet",seek:"missile",anchor:"mines",prism:"beam"};return p[tag]||p[alias[tag]]||0}
function synergyNote(m,player){const active=(m.tags||[]).filter(t=>tagScore(player,t)>0);if(!active.length)return"";const strongest=active.sort((a,b)=>tagScore(player,b)-tagScore(player,a))[0];return `CONNECTS TO ${LABELS[strongest]||strongest.toUpperCase()} · ${tagScore(player,strongest)} MODULE${tagScore(player,strongest)===1?"":"S"} ACTIVE`}
export function renderModuleChoice(button,m,player){const tags=(m.tags||[]).slice(0,4),rows=effects(m),note=synergyNote(m,player);button.className=`choice module-choice rarity-${(m.rarity||"common").toLowerCase()}`;button.innerHTML=`<div class="module-choice-top"><span class="module-rarity">${m.rarity||"COMMON"}</span><span class="module-select">SELECT</span></div><div class="module-choice-main"><div class="module-glyph">${ICONS[tags[0]]||"◇"}</div><div class="module-copy"><b>${m.name}</b><div class="module-tags">${tags.map(t=>`<span>${LABELS[t]||t.toUpperCase()}</span>`).join("")}</div></div></div><div class="module-effects">${rows.map(([tag,label,value])=>`<div><i>${ICONS[tag]||"·"}</i><span>${label}</span><strong>${value}</strong></div>`).join("")}</div>${note?`<div class="module-synergy">${note}</div>`:""}<small class="module-flavor">${m.desc}</small>`}
