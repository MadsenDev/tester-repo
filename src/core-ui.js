import {CORE_UPGRADES,loadCore,buyCore,coreCost} from "./core.js";
const panel=document.querySelector("#core"),grid=document.querySelector("#coreGrid"),shards=document.querySelector("#coreShards"),open=document.querySelector("#openCore"),overlay=document.querySelector("#overlay");
function render(){const core=loadCore();shards.textContent=core.shards;grid.innerHTML="";for(const u of CORE_UPGRADES){const level=core.levels[u.id]||0,cost=coreCost(level),maxed=level>=u.max,b=document.createElement("button");b.className="core-upgrade";b.disabled=maxed||core.shards<cost;b.innerHTML=`<span><strong>${u.name}</strong><small>${u.desc}</small></span><span class="core-level">${level}/${u.max}<small>${maxed?"MAX":cost+" SHARD"+(cost===1?"":"S")}</small></span><i style="--core-progress:${level/u.max*100}%"></i><em>${u.value}</em>`;b.onclick=()=>{buyCore(u.id);render()};grid.appendChild(b)}}
open?.addEventListener("click",()=>{document.querySelectorAll(".panel").forEach(p=>p.classList.add("hidden"));panel.classList.remove("hidden");overlay.classList.add("show");render()});
panel?.querySelector(".core-back")?.addEventListener("click",()=>{panel.classList.add("hidden");document.querySelector("#menu").classList.remove("hidden")});
render();
